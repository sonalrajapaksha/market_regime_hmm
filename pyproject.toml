[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "market-regime-detection"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy", "pandas", "yfinance", "fastapi", "uvicorn[standard]", "prometheus-client", "redis", "mlflow"]

[project.optional-dependencies]
dev = ["pytest", "httpx2", "ruff==0.15.17", "streamlit"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
